import { EXPENSE_CATEGORIES } from '@/lib/expenseCategories';
const escCSV=v=>{if(v==null)return'';const s=String(v);return s.includes(',')||s.includes('"')||s.includes('\n')?`"${s.replace(/"/g,'""')}"`:s};const download=(blob,name)=>{const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();URL.revokeObjectURL(url)};
export function downloadCSV(rows,filename){if(!rows.length)return;const h=Object.keys(rows[0]),lines=[h.join(','),...rows.map(r=>h.map(key=>escCSV(r[key])).join(','))];download(new Blob(['\uFEFF'+lines.join('\n')],{type:'text/csv;charset=utf-8;'}),`${filename}.csv`)}
const escXML=v=>v==null?'':String(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
export function downloadXLSX(rows,filename,sheetName='Hoja1'){if(!rows.length)return;const h=Object.keys(rows[0]),xmlRows=[`<Row>${h.map(x=>`<Cell><Data ss:Type="String">${escXML(x)}</Data></Cell>`).join('')}</Row>`,...rows.map(r=>`<Row>${h.map(x=>`<Cell><Data ss:Type="${typeof r[x]==='number'?'Number':'String'}">${escXML(r[x])}</Data></Cell>`).join('')}</Row>`)].join(''),xml=`<?xml version="1.0" encoding="UTF-8"?>\n<?mso-application progid="Excel.Sheet"?>\n<Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Worksheet ss:Name="${escXML(sheetName)}"><Table>${xmlRows}</Table></Worksheet></Workbook>`;download(new Blob([xml],{type:'application/vnd.ms-excel;charset=utf-8;'}),`${filename}.xls`)}
export const invoicesToRows=invoices=>invoices.map(i=>({'Nº Factura':i.invoice_number||'',Tipo:i.type||'',Estado:i.status||'',Fecha:i.date||'',Vencimiento:i.due_date||'',Trimestre:i.quarter||'','Año Fiscal':i.fiscal_year||'','Cliente/Proveedor':i.client_name||'',NIF:i.client_nif||'',Concepto:i.concept||'','Base Imponible':i.base_imponible??0,'IVA (%)':i.iva_rate??0,'Cuota IVA':i.iva_amount??0,'IRPF (%)':i.irpf_rate??0,'Retención IRPF':i.irpf_amount??0,Total:i.total??0,'Forma de pago':i.payment_method||'',Categoría:i.category?(EXPENSE_CATEGORIES.find(c=>c.value===i.category)?.label||i.category):''}));
export const modelo303ToRows=(rows,year)=>rows.map(r=>({Trimestre:r.label,'Año':year,'Base IVA devengado':r.baseRepercutido,'Cuota IVA devengado':r.cuotaRepercutido,'Base IVA deducible':r.baseSoportado,'Cuota IVA deducible':r.cuotaSoportado,'Resultado Mod. 303':r.resultado}));
export const quarterDataToRows=(rows,year)=>rows.map(q=>({Trimestre:q.fullName,'Año':year,'Ingresos (Base)':q.ingresos,'Gastos (Base)':q.gastos,'IVA Repercutido':q.ivaRepercutido,'IVA Soportado':q.ivaSoportado,'Resultado IVA':q.resultado,'Nº Facturas':q.numFacturas}));
