import React from 'react'; import { Button } from '@/components/ui/button'; import { DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuTrigger } from '@/components/ui/dropdown-menu'; import { Download,FileSpreadsheet,FileText } from 'lucide-react'; import { downloadCSV,downloadXLSX } from '@/lib/exportUtils';
export default function ExportButton({rows,filename,sheetName,disabled}){return <DropdownMenu><DropdownMenuTrigger asChild><Button variant="outline" size="sm" className="gap-1.5" disabled={disabled||!rows?.length}><Download className="w-4 h-4"/>Exportar</Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={()=>downloadXLSX(rows,filename,sheetName)}><FileSpreadsheet className="w-4 h-4 mr-2 text-emerald-600"/>Exportar Excel (.xls)</DropdownMenuItem><DropdownMenuItem onClick={()=>downloadCSV(rows,filename)}><FileText className="w-4 h-4 mr-2 text-primary"/>Exportar CSV (.csv)</DropdownMenuItem></DropdownMenuContent></DropdownMenu>}
