import * as React from 'react'; import * as P from '@radix-ui/react-scroll-area'; import { cn } from '@/lib/utils';
const ScrollArea=React.forwardRef(({className,children,...props},ref)=><P.Root ref={ref} className={cn('relative overflow-hidden',className)} {...props}><P.Viewport className="h-full w-full rounded-[inherit]">{children}</P.Viewport><ScrollBar/><P.Corner/></P.Root>);ScrollArea.displayName=P.Root.displayName;const ScrollBar=React.forwardRef(({className,orientation='vertical',...props},ref)=><P.ScrollAreaScrollbar ref={ref} orientation={orientation} className={cn('flex touch-none select-none transition-colors',orientation==='vertical'&&'h-full w-2.5 border-l border-l-transparent p-[1px]',orientation==='horizontal'&&'h-2.5 flex-col border-t border-t-transparent p-[1px]',className)} {...props}><P.ScrollAreaThumb className="relative flex-1 rounded-full bg-border"/></P.ScrollAreaScrollbar>);ScrollBar.displayName=P.ScrollAreaScrollbar.displayName;export {ScrollArea,ScrollBar};
