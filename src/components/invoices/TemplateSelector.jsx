import React, { useState } from 'react';
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { appClient } from '@/api/appClient';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Input } from '@/components/ui/input'; import { Badge } from '@/components/ui/badge';
import { LayoutTemplate, Trash2, ArrowUpRight, ArrowDownLeft, Search } from 'lucide-react'; import { toast } from '@/components/ui/use-toast';
export default function TemplateSelector({open,onClose,onSelect}){const qc=useQueryClient(),[search,setSearch]=useState('');const {data:templates=[],isLoading}=useQuery({queryKey:['invoice-templates'],queryFn:()=>appClient.entities.InvoiceTemplate.list('name',100),enabled:open});const del=useMutation({mutationFn:id=>appClient.entities.InvoiceTemplate.delete(id),onSuccess:()=>{qc.invalidateQueries({queryKey:['invoice-templates']});toast({title:'Plantilla eliminada'})}});const filtered=templates.filter(t=>!search||t.name?.toLowerCase().includes(search.toLowerCase())||t.client_name?.toLowerCase().includes(search.toLowerCase()));return <Dialog open={open} onOpenChange={onClose}><DialogContent className="max-w-lg"><DialogHeader><DialogTitle className="flex items-center gap-2"><LayoutTemplate className="w-5 h-5 text-primary"/>Plantillas de factura</DialogTitle></DialogHeader><div className="mt-2 space-y-3"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground"/><Input placeholder="Buscar plantilla..." value={search} onChange={e=>setSearch(e.target.value)} className="pl-9"/></div>{isLoading?<p className="text-sm text-muted-foreground text-center py-6">Cargando...</p>:filtered.length===0?<div className="text-center py-10"><LayoutTemplate className="w-10 h-10 text-muted-foreground/30 mx-auto mb-2"/><p className="text-sm text-muted-foreground">{search?'No se encontraron plantillas':'Aún no tienes plantillas guardadas'}</p><p className="text-xs text-muted-foreground mt-1">Rellena una factura y usa "Guardar como plantilla"</p></div>:<div className="space-y-2 max-h-96 overflow-y-auto pr-1">{filtered.map(t=><div key={t.id} className="flex items-center justify-between p-3 rounded-lg border border-border hover:border-primary/40 hover:bg-muted/30 transition-colors cursor-pointer group" onClick={()=>{onSelect(t);onClose()}}><div className="flex items-center gap-3 min-w-0"><div className={`w-7 h-7 rounded-full flex items-center justify-center shrink-0 ${t.type==='emitida'?'bg-emerald-50':'bg-amber-50'}`}>{t.type==='emitida'?<ArrowUpRight className="w-3.5 h-3.5 text-emerald-600"/>:<ArrowDownLeft className="w-3.5 h-3.5 text-amber-600"/>}</div><div className="min-w-0"><p className="text-sm font-medium truncate">{t.name}</p><p className="text-xs text-muted-foreground truncate">{t.client_name||'Sin cliente'}{t.concept?` · ${t.concept}`:''}</p></div></div><div className="flex items-center gap-2 ml-2 shrink-0"><Badge variant="outline" className="text-[10px]">{t.items?.length||0} {t.items?.length===1?'línea':'líneas'}</Badge><button onClick={e=>{e.stopPropagation();del.mutate(t.id)}} className="p-1.5 rounded hover:bg-destructive/10 text-muted-foreground hover:text-destructive opacity-0 group-hover:opacity-100 transition-opacity"><Trash2 className="w-3.5 h-3.5"/></button></div></div>)}</div>}</div></DialogContent></Dialog>}
